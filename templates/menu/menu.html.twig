<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm navbar-custom sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand navbar-brand-lockup" href="{{ path('app_home') }}">
            <img src="{{ asset('image/logo2-transparent.png') }}" alt="Logo Lyceo Campus" class="navbar-logo-owl">
            <span class="brand-lyceo" aria-label="LYCEO Campus Numérique">
                <span class="brand-lyceo-top">LYCEO</span>
                <span class="brand-lyceo-bottom">CAMPUS NUMERIQUE</span>
            </span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'app_home' ? 'active' : '' }}"
                       href="{{ path('app_home') }}">
                        <i class="bi bi-house-door me-1"></i> Accueil
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') starts with 'app_formation' ? 'active' : '' }}"
                       href="{{ path('app_formation') }}">
                        <i class="bi bi-book me-1"></i> Formations
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}#contact">
                        <i class="bi bi-envelope me-1"></i> Contact
                    </a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_admin') }}">
                            <i class="bi bi-gear-fill me-1"></i> Administration
                        </a>
                    </li>
                {% endif %}
            </ul>

            <ul class="navbar-nav">
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle user-dropdown d-inline-flex align-items-center" href="#" id="userDropdown" role="button"
                           data-bs-toggle="dropdown" aria-expanded="false">
                            <span class="user-avatar me-2">
                                <i class="bi bi-person-circle"></i>
                            </span>
                            <span class="user-name">{{ app.user.email|split('@')[0] }}</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end custom-dropdown" aria-labelledby="userDropdown">
                            <li>
                                <a class="dropdown-item" href="{{ path('app_mon_profil') }}">
                                    <i class="bi bi-person me-2"></i> Mon Profil
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                    <i class="bi bi-box-arrow-right me-2"></i> Déconnexion
                                </a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="btn btn-primary btn-login" href="{{ path('app_login') }}">
                            <i class="bi bi-box-arrow-in-right me-1"></i> Connexion
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

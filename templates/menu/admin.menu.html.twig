<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm navbar-admin">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="{{ path('app_admin') }}">
            <i class="bi bi-shield-lock-fill" aria-hidden="true"></i>
            <span>Administration</span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNav"
                aria-controls="adminNav" aria-expanded="false" aria-label="Menu administration">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="adminNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link admin-nav-link {{ app.request.attributes.get('_route') == 'app_admin' ? 'active' : '' }}"
                       href="{{ path('app_admin') }}">
                        <i class="bi bi-speedometer2 admin-nav-icon" aria-hidden="true"></i>
                        <span class="admin-nav-label">Dashboard</span>
                    </a>
                </li>
                {% if is_granted('ROLE_SUPER_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link admin-nav-link {{ app.request.attributes.get('_route') == 'app_admin_home_page_edit' ? 'active' : '' }}"
                       href="{{ path('app_admin_home_page_edit') }}">
                        <i class="bi bi-house-door admin-nav-icon" aria-hidden="true"></i>
                        <span class="admin-nav-label">Page d'accueil</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link admin-nav-link {{ app.request.attributes.get('_route') == 'app_admin_home_promo' ? 'active' : '' }}"
                       href="{{ path('app_admin_home_promo') }}">
                        <i class="bi bi-play-btn admin-nav-icon" aria-hidden="true"></i>
                        <span class="admin-nav-label">Média d'accueil</span>
                    </a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link admin-nav-link {{ app.request.attributes.get('_route') starts with 'app_admin_contact' ? 'active' : '' }}"
                       href="{{ path('app_admin_contact_index') }}">
                        <i class="bi bi-envelope admin-nav-icon" aria-hidden="true"></i>
                        <span class="admin-nav-label">Messages contact</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link admin-nav-link {{ app.request.attributes.get('_route') starts with 'app_categorie' ? 'active' : '' }}"
                       href="{{ path('app_categorie_index') }}">
                        <i class="bi bi-tags admin-nav-icon" aria-hidden="true"></i>
                        <span class="admin-nav-label">Catégories</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link admin-nav-link {{ app.request.attributes.get('_route') starts with 'app_admin_formation' ? 'active' : '' }}"
                       href="{{ path('app_admin_formation_index') }}">
                        <i class="bi bi-book admin-nav-icon" aria-hidden="true"></i>
                        <span class="admin-nav-label">Formations&nbsp;&amp;&nbsp;Chapitres</span>
                    </a>
                </li>
                {% if is_granted('ROLE_SUPER_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link admin-nav-link {{ app.request.attributes.get('_route') == 'app_admin_payments' ? 'active' : '' }}"
                       href="{{ path('app_admin_payments') }}">
                        <i class="bi bi-credit-card admin-nav-icon" aria-hidden="true"></i>
                        <span class="admin-nav-label">Paiements</span>
                    </a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link admin-nav-link {{ app.request.attributes.get('_route') == 'app_admin_stats' ? 'active' : '' }}"
                       href="{{ path('app_admin_stats') }}">
                        <i class="bi bi-graph-up admin-nav-icon" aria-hidden="true"></i>
                        <span class="admin-nav-label">Statistiques</span>
                    </a>
                </li>
                {% if is_granted('ROLE_SUPER_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link admin-nav-link {{ app.request.attributes.get('_route') starts with 'app_admin_user' ? 'active' : '' }}"
                       href="{{ path('app_admin_user_index') }}">
                        <i class="bi bi-person-gear admin-nav-icon" aria-hidden="true"></i>
                        <span class="admin-nav-label">Administrateurs</span>
                    </a>
                </li>
                {% endif %}
            </ul>

            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link admin-nav-link" href="{{ path('app_home') }}">
                        <i class="bi bi-arrow-left-circle admin-nav-icon" aria-hidden="true"></i>
                        <span class="admin-nav-label">Retour au site</span>
                    </a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link admin-nav-link dropdown-toggle" href="#" id="adminUserDropdown" role="button"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-person-circle admin-nav-icon" aria-hidden="true"></i>
                        <span class="admin-nav-label">{{ app.user.email|split('@')[0] }}</span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="adminUserDropdown">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_mon_profil') }}">
                                <i class="bi bi-person"></i> Mon Profil
                            </a>
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                <i class="bi bi-box-arrow-right"></i> Déconnexion
                            </a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
